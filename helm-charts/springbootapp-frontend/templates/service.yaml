apiVersion: v1
kind: Service
metadata:
  name: {{ include "springbootapp-chart.fullname" . }}
  labels:
    {{- include "springbootapp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "springbootapp-chart.selectorLabels" . | nindent 4 }}
