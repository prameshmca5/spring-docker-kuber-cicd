# Default values for springbootapp-db.
# This is a YAML-formatted file.

replicaCount: 1

image:
  repository: postgres
  pullPolicy: IfNotPresent
  tag: "15"

serviceAccount:
  create: false
  name: ""

postgres:
  enabled: true
  database: employee_db
  username: postgres
  password: root
  port: 5432
