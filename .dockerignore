# Exclude build output — Maven produces the JAR inside the container, not from host
target/

# IDE and OS files
.idea/
*.iml
.DS_Store
*.swp

# Git
.git/
.gitignore
.gitattributes

# Docker files themselves (not needed inside the image)
Dockerfile
docker-compose.yml
.dockerignore

# Docs
*.md
HELP.md
